# Windows クライアント側の依存（Python 3.10+ 推奨）
#
# faster-whisper を CUDA で動かすには、別途 NVIDIA の cuBLAS / cuDNN が必要です。
# 一番手軽なのは pip で同梱版を入れる方法（下2行）。うまく動かない場合は
# CUDA Toolkit 12.x + cuDNN 9 を手動導入してください。
nvidia-cublas-cu12
nvidia-cudnn-cu12

faster-whisper>=1.0.0
openwakeword>=0.6.0          # ウェイクワード検出（OSS, onnxruntime 同梱）
pvrecorder>=1.2.0           # マイク録音のみ（Apache-2.0, キー不要）
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24
requests>=2.31
